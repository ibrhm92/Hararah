# اختبار نظام الإشعارات 🧪
## Test Guide for News Notification System

---

## ✅ قائمة الاختبار

### الاختبار 1: إذن الإشعارات
```javascript
// 1. افتح DevTools (F12) → Console
// 2. اكتب:
Notification.permission

// النتيجة المتوقعة:
// "granted" ✅ - الإذن مُعطى
// "denied" ❌ - الإذن مرفوض
// "default" ⚠️ - لم يُطلب بعد
```

---

### الاختبار 2: الإشعار الخارجي
```javascript
// في Console:
sendBrowserNotification('اختبار الإشعار', {
    body: 'هذا اختبار لنظام الإشعارات',
    icon: '📰',
    requireInteraction: false
});

// يجب أن تظهر نافذة بوب آب فوراً ✓
```

---

### الاختبار 3: الإشعار الداخلي
```javascript
// في Console:
addNotification(
    'خبر اختبار',
    'هذا اختبار لوحة الإشعارات',
    'news'
);

// تحقق:
// 1. رقم احمر على زر الجرس (1) ✓
// 2. افتح لوحة الإشعارات ✓
// 3. يجب أن تري الإشعار الجديد ✓
```

---

### الاختبار 4: localStorage
```javascript
// تحقق من حفظ الإشعارات:
JSON.parse(localStorage.getItem('notifications'))

// يجب أن ترى المصفوفة بالإشعارات ✓
// جرّب إعادة تحميل الصفحة
// يجب أن تبقى الإشعارات ✓
```

---

### الاختبار 5: مراقبة الأخبار
```javascript
// تحقق من حالة المراقبة:
newsCheckInterval  // يجب أن يكون رقماً

// جرب الفحص اليدوي:
await checkForNewNews();

// تحقق من آخر وقت فحص:
new Date(lastNewsCheckTime).toLocaleString('ar-SA')
```

---

### الاختبار 6: تحديث الأخبار
```javascript
// في Console:
const news = await getData('news');
console.log('الأخبار:', news);

// جرب تحديث العرض:
updateLatestNewsDisplay(news);

// تحقق:
// 1. يجب أن تظهر أحدث 3 أخبار على الصفحة الرئيسية ✓
// 2. الأخبار العاجلة يجب أن تظهر بأحمر ✓
```

---

## 🧪 اختبار عملي كامل

### الخطوة 1: تحضير الاختبار

```javascript
// افتح التطبيق → DevTools → Console
// انسخ والصق هذا الكود:

console.log('=== بدء اختبار نظام الإشعارات ===');

// 1. التحقق من المتغيرات
console.log('✓ Notifications array:', notifications.length, 'إشعار');
console.log('✓ Notification count:', notificationCount);
console.log('✓ Permission:', Notification.permission);
console.log('✓ Monitor interval:', newsCheckInterval ? 'مُشغّل' : 'معطّل');

// 2. اختبر الإشعار الخارجي
sendBrowserNotification('🧪 اختبار', {body: 'اختبار الإشعارات'});

// 3. اختبر الإشعار الداخلي
addNotification('اختبار داخلي', 'اختبار لوحة الإشعارات', 'news');

// 4. تحقق من التخزين
const stored = localStorage.getItem('notifications');
console.log('✓ Stored notifications:', stored ? 'موجودة ✓' : 'غير موجودة ❌');

console.log('=== انتهى الاختبار ===');
```

---

### الخطوة 2: إضافة خبر اختبار إلى Firebase

```javascript
// في Console Admin (إذا كان مسموحاً):
const testNews = {
    title: 'خبر اختبار جديد',
    content: 'هذا خبر اختبار لمراقبة الأخبار',
    created_at: new Date().toISOString(),
    urgent: false,
    author: 'تطبيق الاختبار'
};

await saveData('news', testNews);
console.log('✓ تم إضافة الخبر الاختبار');

// بعد 60 ثانية يجب أن تظهر:
// 1. إشعار خارجي ✓
// 2. شارة على الجرس ✓
// 3. إشعار في اللوحة ✓
```

---

### الخطوة 3: اختبر خبر عاجل

```javascript
// في Console Admin:
const urgentNews = {
    title: 'تنبيه عاجل - اختبار',
    content: 'هذا خبر عاجل للاختبار',
    created_at: new Date().toISOString(),
    urgent: true,  // ← المفتاح
    author: 'اختبار'
};

await saveData('news', urgentNews);

// بعد 60 ثانية يجب أن تظهر:
// 1. إشعار خارجي مع ⚠️ عاجل
// 2. نبضات إضاءة مستمرة
// 3. اهتزاز قوي على الهاتف
```

---

## 🔍 أوامر التشخيص

### معلومات عامة
```javascript
console.log({
    totalNotifications: notifications.length,
    unreadCount: notifications.filter(n => !n.read).length,
    permission: Notification.permission,
    lastCheckTime: new Date(lastNewsCheckTime).toLocaleString('ar-SA'),
    monitoringActive: !!newsCheckInterval,
    isLocalStorageAvailable: (() => {
        try {
            localStorage.setItem('test', 'test');
            localStorage.removeItem('test');
            return true;
        } catch(e) {
            return false;
        }
    })()
});
```

---

### معلومات Firebase
```javascript
// تحقق من الاتصال
try {
    const news = await getData('news');
    console.log('✓ Firebase متصل');
    console.log('✓ عدد الأخبار:', news.length);
    console.log('✓ آخر خبر:', news[0]?.title);
} catch(e) {
    console.error('❌ خطأ في Firebase:', e);
}
```

---

### معلومات localStorage
```javascript
// عرض كل الإشعارات المحفوظة
const stored = localStorage.getItem('notifications');
if (stored) {
    console.table(JSON.parse(stored));
} else {
    console.log('لا توجد إشعارات محفوظة');
}

// حجم التخزين
const size = new Blob([stored]).size;
console.log(`حجم البيانات: ${size} بايت`);
```

---

## 🐛 اختبار الأخطاء

### اختبر معالجة الأخطاء
```javascript
// 1. محاولة فشل في Firebase
try {
    await checkForNewNews();
    console.log('✓ معالجة الأخطاء تعمل');
} catch(e) {
    console.log('❌ حدث خطأ:', e);
}

// 2. localStorage ممتلئ
try {
    localStorage.setItem('test', 'x'.repeat(10000000));
} catch(e) {
    console.log('✓ معالجة امتلاء localStorage صحيحة');
}
```

---

## 📊 مؤشرات الأداء

```javascript
// قياس سرعة الفحص
console.time('checkForNewNews');
await checkForNewNews();
console.timeEnd('checkForNewNews');

// النتيجة المتوقعة: < 500ms
```

---

## ✅ قائمة التحقق النهائية

| الميزة | الاختبار | النتيجة |
|--------|---------|--------|
| إذن الإشعارات | Notification.permission | ✓ |
| الإشعار الخارجي | sendBrowserNotification() | ✓ |
| الإشعار الداخلي | addNotification() | ✓ |
| شارة الإشعارات | notification badge | ✓ |
| لوحة الإشعارات | toggleNotificationsPanel() | ✓ |
| التخزين | localStorage | ✓ |
| مراقبة الأخبار | checkForNewNews() | ✓ |
| تحديث الصفحة | updateLatestNewsDisplay() | ✓ |
| الأخبار العاجلة | urgent news styling | ✓ |
| الاهتزاز | vibrate pattern | ✓ |

---

## 🚀 نصائح الاختبار

### نصيحة 1: اختبر مع إغلاق التطبيق
```javascript
// 1. افتح التطبيق
// 2. اختبر الإشعارات
// 3. أغلق التطبيق تماماً
// 4. أضف خبر جديد من لوحة التحكم
// يجب أن تظهر إشعارات المتصفح حتى لو مغلق ✓
```

---

### نصيحة 2: اختبر على هاتف فعلي
```javascript
// 1. استخدم ngrok أو Vercel لعمل رابط عام
// 2. افتح الرابط على الهاتف
// 3. اختبر الاهتزاز والإشعارات
// 4. تحقق من أداء البطارية
```

---

### نصيحة 3: اختبر مع شبكة سيئة
```javascript
// DevTools → Network → Throttle
// اختر "Slow 3G" لمحاكاة شبكة سيئة
// تحقق من:
// 1. الإشعارات تظهر حتى مع تأخير
// 2. لا توجد تكرار للإشعارات
```

---

## 📝 تقرير الاختبار

عند اكتمال الاختبار، استخدم هذا التقرير:

```markdown
# تقرير اختبار نظام الإشعارات

## التاريخ: 
- [ ] 13 يناير 2026

## البيئة:
- [ ] Windows / macOS / Linux
- [ ] Chrome / Firefox / Safari
- [ ] النسخة: ___________

## نتائج الاختبار:

### إشعارات المتصفح
- [ ] تظهر الإشعارات ✓
- [ ] تظهر الصور ✓
- [ ] الصوت يعمل ✓
- [ ] الاهتزاز يعمل ✓

### اللوحة الداخلية
- [ ] تظهر الإشعارات ✓
- [ ] الشارة تحديث صحيح ✓
- [ ] تحديث كمقروء يعمل ✓
- [ ] التخزين يعمل ✓

### الأخبار الجديدة
- [ ] الأخبار تظهر فوراً ✓
- [ ] الأخبار العاجلة تُميز ✓
- [ ] الترتيب صحيح ✓

### الأداء
- [ ] لا تأخير ملحوظ ✓
- [ ] البطارية مستقرة ✓
- [ ] الذاكرة تحت السيطرة ✓

## ملاحظات:
___________

## الحالة:
- [ ] نجح ✅
- [ ] فشل ❌
- [ ] يحتاج تحسينات ⚠️
```

---

**آخر تحديث:** 13 يناير 2026
