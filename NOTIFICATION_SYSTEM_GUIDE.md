# دليل نظام الإشعارات 📱
## Notification System Guide

---

## 📋 محتويات الملف
1. [نظرة عامة](#نظرة-عامة)
2. [آلية العمل](#آلية-العمل)
3. [المكونات الرئيسية](#المكونات-الرئيسية)
4. [وظائف النظام](#وظائف-النظام)
5. [أمثلة الاستخدام](#أمثلة-الاستخدام)
6. [التخصيص](#التخصيص)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

نظام الإشعارات يوفر:
- **إشعارات المتصفح** - Browser Notifications API
- **لوحة الإشعارات** - In-app notification panel
- **التخزين المستمر** - localStorage persistence
- **مراقبة الأخبار الجديدة** - Automatic news monitoring
- **الوقت بالعربية** - Arabic time formatting

**الهدف:** إعلام المستخدمين بالأخبار الجديدة فور نشرها

---

## ⚙️ آلية العمل

### 1. مراحل التشغيل

```
بدء التطبيق
    ↓
تحميل الإشعارات من التخزين (localStorage)
    ↓
طلب إذن الإشعارات من المستخدم
    ↓
بدء مراقبة الأخبار الجديدة (كل 60 ثانية)
    ↓
عند اكتشاف خبر جديد:
    ├─ إضافة إلى لوحة الإشعارات
    ├─ إرسال إشعار المتصفح
    ├─ تحديث الشارة (Badge)
    └─ حفظ في التخزين
```

### 2. دورة حياة الإشعار

```
1️⃣ إنشاء الإشعار
   - المعرف الفريد (ID): timestamp
   - العنوان (Title)
   - الرسالة (Message)
   - النوع (Type): news, info, alert
   - الوقت (Time)
   - حالة القراءة (Read Status)

2️⃣ العرض
   - في لوحة الإشعارات
   - كشارة على زر الإشعارات

3️⃣ التفاعل
   - تحديد كمقروء عند فتح اللوحة
   - يمكن النقر للذهاب للصفحة

4️⃣ التخزين
   - حفظ في localStorage
   - استرجاع عند فتح التطبيق
```

### 3. تدفق المراقبة (Monitoring Flow)

```
كل 60 ثانية:
  ↓
checkForNewNews() يعمل
  ↓
جلب جميع الأخبار من Firebase
  ↓
مقارنة وقت الخبر بـ lastNewsCheckTime
  ↓
إذا كان أحدث ← خبر جديد!
  ↓
├─ addNotification()      (إضافة للوحة)
├─ sendBrowserNotification() (إشعار المتصفح)
└─ updateNotificationBadge() (تحديث الشارة)
```

---

## 🔧 المكونات الرئيسية

### 1. عناصر HTML

**زر الإشعارات:**
```html
<button id="notificationBtn">
    <i class="fas fa-bell"></i>
    <span id="notificationBadge">5</span>
</button>
```

**لوحة الإشعارات:**
```html
<div id="notificationsPanel" class="notifications-panel">
    <div class="notifications-header">
        <h3>الإشعارات</h3>
        <button onclick="clearAllNotifications()">مسح الكل</button>
    </div>
    <div id="notificationsList" class="notifications-list">
        <!-- الإشعارات تُرسم هنا ديناميكياً -->
    </div>
</div>
```

### 2. متغيرات الحالة

```javascript
// مصفوفة الإشعارات
notifications = [];

// عدد الإشعارات
notificationCount = 0;

// وقت آخر فحص للأخبار الجديدة
lastNewsCheckTime = null;

// معرّف الفترة الزمنية للمراقبة
newsCheckInterval = null;

// الإعدادات
CONFIG = {
    NEWS_CHECK_INTERVAL: 60000  // كل 60 ثانية
}
```

### 3. التخزين (localStorage)

```javascript
// مفتاح التخزين
localStorage.setItem('notifications', JSON.stringify(notifications));

// البيانات المخزنة:
{
    id: 1234567890,
    title: "خبر جديد",
    message: "تفاصيل الخبر...",
    type: "news",
    time: "2026-01-13T10:30:00.000Z",
    read: false
}
```

---

## 📞 وظائف النظام

### 1. طلب الإذن

```javascript
/**
 * طلب إذن الإشعارات من المتصفح
 * @returns {Promise<boolean>} - هل تم منح الإذن
 */
async requestNotificationPermission()
```

**مراحل العمل:**
1. التحقق من دعم المتصفح
2. التحقق من الإذن السابق
3. إذا لم يُعطَ → طلب الإذن
4. إرجاع النتيجة

---

### 2. إرسال إشعار المتصفح

```javascript
/**
 * إرسال إشعار نظام المتصفح
 * @param {string} title - عنوان الإشعار
 * @param {object} options - خيارات إضافية
 */
function sendBrowserNotification(title, options = {})
```

**الخيارات:**
```javascript
{
    body: "نص الإشعار",
    icon: "رابط الصورة",
    badge: "رابط شارة صغيرة",
    tag: "معرف فريد لتجميع الإشعارات",
    requireInteraction: false
}
```

**عند النقر:** يُفتح التطبيق ويذهب إلى صفحة الأخبار

---

### 3. إضافة إشعار

```javascript
/**
 * إضافة إشعار جديد إلى لوحة الإشعارات
 * @param {string} title - العنوان
 * @param {string} message - الرسالة
 * @param {string} type - النوع (news, info, alert)
 */
function addNotification(title, message, type = 'news')
```

**العملية:**
1. إنشاء كائن إشعار بمعرف فريد
2. إضافته في بداية المصفوفة
3. زيادة عدّاد الإشعارات
4. تحديث الشارة
5. إعادة رسم اللوحة
6. حفظ في التخزين

---

### 4. تحديث الشارة

```javascript
/**
 * تحديث رقم الإشعارات غير المقروءة
 */
function updateNotificationBadge()
```

**المنطق:**
- عد الإشعارات غير المقروءة
- إظهار العدد على الشارة
- إخفاء الشارة إذا كان العدد = 0
- تأثير بصري: رسوم متحركة (pulse)

---

### 5. عرض الإشعارات

```javascript
/**
 * رسم قائمة الإشعارات في اللوحة
 */
function renderNotifications()
```

**المعلومات المعروضة:**
```
┌─────────────────────────────┐
│ 📰 عنوان الخبر             │
│                             │
│ نص الخبر (أول 100 حرف)    │
│ قبل 5 دقائق                 │
└─────────────────────────────┘
```

---

### 6. تحديد كمقروء

```javascript
/**
 * تحديد إشعار معين كمقروء
 * @param {number} notificationId - معرّف الإشعار
 */
function markNotificationAsRead(notificationId)
```

**يحدث عند:**
- فتح لوحة الإشعارات
- النقر على إشعار معين

---

### 7. مسح الإشعارات

```javascript
/**
 * مسح جميع الإشعارات
 */
function clearAllNotifications()
```

---

### 8. التحكم باللوحة

```javascript
/**
 * إظهار/إخفاء لوحة الإشعارات
 */
function toggleNotificationsPanel()

/**
 * إغلاق لوحة الإشعارات فقط
 */
function closeNotificationsPanel()
```

---

### 9. التخزين والتحميل

```javascript
/**
 * حفظ الإشعارات في localStorage
 */
function saveNotificationsToStorage()

/**
 * تحميل الإشعارات من localStorage
 */
function loadNotificationsFromStorage()
```

**الفائدة:** عدم فقدان الإشعارات عند إغلاق التطبيق

---

### 10. الوقت بالعربية

```javascript
/**
 * تحويل الوقت إلى صيغة عربية نسبية
 * @param {Date} date - التاريخ
 * @returns {string} - "الآن" أو "قبل 5 دقائق" إلخ
 */
function getTimeAgoArabic(date)
```

**الصيغ:**
- `الآن` - أقل من دقيقة
- `قبل 5 دقائق` - أقل من ساعة
- `قبل 2 ساعة` - أقل من يوم
- `قبل 3 أيام` - أقل من 30 يوم
- التاريخ الكامل - أكثر من 30 يوم

---

### 11. فحص الأخبار الجديدة

```javascript
/**
 * فحص وجود أخبار جديدة
 */
async function checkForNewNews()
```

**الخطوات:**
1. جلب جميع الأخبار من Firebase
2. المقارنة مع `lastNewsCheckTime`
3. إذا وجدت أخبار جديدة:
   - إنشاء إشعار
   - إرسال إشعار المتصفح
4. تحديث `lastNewsCheckTime`

---

### 12. مراقبة الأخبار

```javascript
/**
 * بدء مراقبة الأخبار الجديدة
 * يعمل كل 60 ثانية
 */
function startNewsMonitoring()

/**
 * إيقاف مراقبة الأخبار
 */
function stopNewsMonitoring()
```

---

## 💡 أمثلة الاستخدام

### مثال 1: إضافة إشعار يدويٍ

```javascript
// في لوحة التحكم عند نشر خبر
addNotification(
    "خبر جديد: فتح طريق جديد",
    "تم افتتاح الطريق الرابطة بين القرية والبلدة المجاورة",
    "news"
);
```

### مثال 2: إرسال إشعار متقدم

```javascript
sendBrowserNotification("خبر عاجل", {
    body: "تحديث جديد متوفر",
    icon: "🏘️",
    badge: "📰",
    requireInteraction: true  // المستخدم يجب أن يتفاعل
});
```

### مثال 3: فحص يدوي للأخبار

```javascript
// زر "فحص الأخبار" في التطبيق
document.getElementById('checkNewsBtn').addEventListener('click', async () => {
    showLoadingOverlay();
    await checkForNewNews();
    hideLoadingOverlay();
});
```

### مثال 4: عرض الإشعارات

```javascript
// عند النقر على زر الإشعارات
document.getElementById('notificationBtn').addEventListener('click', () => {
    toggleNotificationsPanel();
});
```

---

## 🎨 التخصيص

### 1. تغيير فترة المراقبة

```javascript
// في script-firebase-complete.js أو script-firebase-fixed.js

CONFIG = {
    ...
    NEWS_CHECK_INTERVAL: 30000  // كل 30 ثانية بدلاً من 60
    ...
}
```

### 2. تغيير أيقونة الإشعار

```javascript
// في دالة sendBrowserNotification()

const notification = new Notification(title, {
    icon: 'رابط صورة مخصصة.png',
    badge: 'رابط شارة مخصصة.png',
    ...
});
```

### 3. تخصيص نص اللوحة الفارغة

```javascript
// في دالة renderNotifications()

notificationsList.innerHTML = `
    <div class="notifications-empty">
        <i class="fas fa-inbox"></i>
        <p>لا توجد إشعارات حالياً</p>  {/* عدّل النص هنا */}
    </div>
`;
```

### 4. تخصيص الألوان والتأثيرات

```css
/* في styles.css */

/* شارة الإشعارات */
#notificationBadge {
    background: #ff4444;  /* اللون الأحمر */
}

/* اللوحة */
.notifications-panel {
    background: white;
    width: 360px;
}

/* الإشعار غير المقروء */
.notification-item.unread {
    background: #f0f0ff;
    border-right: 4px solid #007bff;
}
```

---

## 🔍 استكشاف الأخطاء

### المشكلة 1: الإشعارات لا تظهر

**الأسباب المحتملة:**
1. ✓ المتصفح لا يدعم الإشعارات
2. ✓ المستخدم رفض الإذن
3. ✓ JavaScript معطل

**الحل:**
```javascript
// تحقق من السجل (Console)
console.log('Notification permission:', Notification.permission);
console.log('Notifications array:', notifications);
```

---

### المشكلة 2: الإشعارات تختفي عند إعادة التحميل

**السبب:** `localStorage` قد يكون معطلاً

**الحل:**
```javascript
// تحقق من localStorage
try {
    localStorage.setItem('test', 'test');
    localStorage.removeItem('test');
    console.log('localStorage متاح');
} catch(e) {
    console.error('localStorage غير متاح:', e);
}
```

---

### المشكلة 3: المراقبة لا تعمل

**تحقق من:**
```javascript
// 1. هل startNewsMonitoring() تم استدعاؤها؟
console.log('Interval ID:', newsCheckInterval);

// 2. هل هناك خطأ في Firebase؟
try {
    const news = await getData('news');
    console.log('News data:', news);
} catch(e) {
    console.error('Firebase error:', e);
}

// 3. هل آخر وقت فحص صحيح؟
console.log('Last check time:', lastNewsCheckTime);
```

---

### المشكلة 4: الإشعارات متكررة

**السبب:** `lastNewsCheckTime` لم يُحدث

**الحل:**
```javascript
// إعادة تعيين وقت الفحص
lastNewsCheckTime = Date.now();
localStorage.removeItem('notifications');
```

---

## 📊 مثال كامل للاستخدام

```javascript
// 1️⃣ عند بدء التطبيق (يتم تلقائياً)
loadNotificationsFromStorage();
requestNotificationPermission();
startNewsMonitoring();

// 2️⃣ عند نشر خبر جديد (من لوحة التحكم)
const newsItem = {
    title: "خبر جديد",
    content: "محتوى الخبر...",
    created_at: new Date().toISOString()
};

// سيتم اكتشافه تلقائياً في الفحص التالي
await saveData('news', newsItem);

// 3️⃣ أو إضافة يدويٍ
addNotification(
    newsItem.title,
    newsItem.content.substring(0, 100) + '...',
    'news'
);

// 4️⃣ المستخدم يرى:
// - شارة حمراء مع الرقم "1"
// - إشعار المتصفح (إذا كان التطبيق مغلقاً)
// - الإشعار في اللوحة (عند فتح التطبيق)

// 5️⃣ عند النقر على الإشعار:
// - تحديده كمقروء
// - يمكن الذهاب إلى صفحة الأخبار
```

---

## 🚀 الميزات المتقدمة

### 1. إشعارات مجمعة

يمكن تعديل `sendBrowserNotification()` لتجميع إشعارات متعددة:

```javascript
// استخدام نفس tag للتجميع
function sendBrowserNotification(title, options = {}) {
    return new Notification(title, {
        tag: 'hararah-news',  // نفس المعرف = تجميع
        ...options
    });
}
```

### 2. إشعارات صوتية

```javascript
function sendBrowserNotification(title, options = {}) {
    return new Notification(title, {
        sound: 'notification-sound.mp3',
        ...options
    });
}
```

### 3. إشعارات بأيقونات مختلفة

```javascript
const icons = {
    'news': '📰',
    'alert': '⚠️',
    'success': '✅',
    'info': 'ℹ️'
};

function getNotificationIcon(type) {
    return `data:image/svg+xml,<svg>...${icons[type]}...</svg>`;
}
```

---

## 📝 ملاحظات مهمة

⚠️ **إذن المستخدم مهم:**
- اطلب الإذن في البداية فقط
- لا تطلبه بشكل متكرر

⚠️ **الخصوصية:**
- الإشعارات قد تظهر على شاشة القفل
- لا تضع معلومات حساسة فيها

⚠️ **الأداء:**
- المراقبة تعمل كل 60 ثانية (قابل للتخصيص)
- استخدم فترات أطول إذا كان هناك ضغط على النظام

⚠️ **التوافقية:**
- متوفر في جميع المتصفحات الحديثة
- في المتصفحات القديمة، تعمل لوحة الإشعارات فقط

---

## 📞 للدعم والمساعدة

إذا واجهت أي مشاكل:
1. افتح Browser DevTools (F12)
2. انظر إلى Console للأخطاء
3. تحقق من:
   - `notifications` array
   - `localStorage`
   - `Firebase connection`
   - `Notification permission`

---

**آخر تحديث:** 13 يناير 2026
**الإصدار:** 1.0
