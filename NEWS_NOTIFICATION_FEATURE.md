# ميزة الإشعارات الفورية للأخبار الجديدة 📰🔔
## News Notification Feature Guide

---

## 📋 المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [آلية العمل](#آلية-العمل)
3. [أنواع الإشعارات](#أنواع-الإشعارات)
4. [المميزات](#المميزات)
5. [كيفية الاستخدام](#كيفية-الاستخدام)
6. [التخصيص والتطوير](#التخصيص-والتطوير)
7. [حل المشاكل](#حل-المشاكل)

---

## 🎯 نظرة عامة

هذه الميزة توفر **إشعارات فورية وفعالة** عند نزول خبر جديد على التطبيق:

✅ **إشعار خارجي** - البوب آب على الهاتف/الكمبيوتر  
✅ **لوحة إشعارات داخلية** - تتبع الأخبار في التطبيق  
✅ **تحديث الأخبار** - عرض الأخبار الجديدة فوراً  
✅ **أخبار عاجلة** - تنبيهات مستمرة للأخبار الحرجة  
✅ **التخزين المستمر** - الإشعارات لا تُفقد  

---

## ⚙️ آلية العمل

### 1. دورة المراقبة الكاملة

```
بدء التطبيق (DOMContentLoaded)
    ↓
تحميل الإشعارات السابقة من localStorage
    ↓
طلب إذن الإشعارات من المستخدم
    ↓
بدء مراقبة الأخبار (كل 60 ثانية)
    │
    ├─→ checkForNewNews() عند الفحص الأول
    │
    ├─→ كل 60 ثانية: checkForNewNews() تعمل
    │   │
    │   ├─→ جلب جميع الأخبار من Firebase
    │   │
    │   ├─→ مقارنة تاريخ الخبر مع آخر فحص (lastNewsCheckTime)
    │   │
    │   ├─→ إذا كان الخبر أحدث ← خبر جديد!
    │   │
    │   ├─→ forEach (كل خبر جديد):
    │   │   ├─ إضافة للوحة الإشعارات الداخلية
    │   │   ├─ إرسال إشعار المتصفح/الهاتف
    │   │   └─ تحديث الأخبار على الصفحة الرئيسية
    │   │
    │   └─→ تحديث آخر وقت فحص
    │
    └─→ التكرار كل 60 ثانية...
```

### 2. ما يحدث عند اكتشاف خبر جديد

```
checkForNewNews() يكتشف خبر جديد
    ↓
addNotification()
├─ إضافة الخبر إلى مصفوفة notifications[]
├─ زيادة عدّاد notificationCount
├─ تحديث شارة الإشعارات (الرقم الأحمر)
├─ إعادة رسم لوحة الإشعارات
└─ حفظ في localStorage
    ↓
sendBrowserNotification()
├─ عرض إشعار نظام المتصفح/الهاتف
├─ عرض صورة الخبر (إن وجدت)
├─ عرض النص الأساسي
├─ تفعيل الاهتزاز (Vibration API للهواتف)
└─ عند النقر: تحديثه كمقروء + الذهاب للخبر
    ↓
updateLatestNewsDisplay()
├─ تحديث آخر 3 أخبار على الصفحة الرئيسية
├─ ترتيب الأخبار حسب التاريخ
├─ عرض أيقونة "عاجل" للأخبار الحرجة
└─ إظهار زر "عرض المزيد"
```

---

## 📬 أنواع الإشعارات

### 1. إشعار عادي (Normal News)

```javascript
{
    title: "تطوير الطريق الرابطة بين القرية",
    message: "تم بدء أعمال الصيانة والتطوير...",
    type: "news",
    urgent: false,
    icon: "📰",
    vibrate: [200, 100, 200]  // أنماط اهتزاز
}
```

**المظهر:**
- خط رمادي على اليمين
- خلفية بيضاء عادية
- لا يوجد تأثير متحرك

---

### 2. خبر عاجل (Urgent News)

```javascript
{
    title: "تنبيه أمني عاجل",
    message: "يُطلب من الجميع توخي الحذر...",
    type: "news",
    urgent: true,  // ← الفرق الأساسي
    icon: "⚠️",
    vibrate: [300, 100, 300, 100, 300]  // اهتزاز أقوى
}
```

**المظهر:**
```
┌─────────────────────────────────────┐
│ ⚠️ عاجل - تنبيه أمني عاجل          │  ← أيقونة عاجلة
│                                     │
│ يُطلب من الجميع توخي الحذر...      │
│                                     │  ← خط أحمر
└─────────────────────────────────────┘

التأثيرات:
- خط أحمر على اليمين (4px)
- تدرج أحمر خفيف في الخلفية
- نبضات إضاءة مستمرة
- اهتزاز قوي على الهاتف
```

**السلوك:**
- `requireInteraction: true` - المستخدم يجب أن يتفاعل
- نبضات إضاءة مستمرة
- اهتزاز متكرر حتى يُغلق

---

### 3. الإشعار في لوحة التطبيق

```
الإشعارات
────────────────────────────
📰 خبر جديد
└─ النص الأساسي...
   قبل 2 دقيقة
────────────────────────────
⚠️ تنبيه أمني عاجل
└─ يُطلب من الجميع...
   الآن
────────────────────────────
(مسح الكل) ← زر
```

---

## ⭐ المميزات

### 1. الإشعارات الخارجية (Browser Notifications)

```javascript
// تظهر حتى لو كان التطبيق مغلقاً
sendBrowserNotification('📰 خبر جديد', {
    body: 'تفاصيل الخبر...',
    icon: newsItem.image || '📰.svg',
    badge: '📰.svg',
    tag: 'news-' + newsItem.id,
    requireInteraction: newsItem.urgent,
    vibrate: [200, 100, 200]  // ← اهتزاز الهاتف
});
```

**الفوائد:**
- ✅ تظهر حتى لو التطبيق مغلق
- ✅ صوت تنبيه تلقائي
- ✅ اهتزاز على الهواتف
- ✅ يمكن النقر للفتح
- ✅ تظهر على شاشة القفل

---

### 2. لوحة الإشعارات الداخلية

```javascript
toggleNotificationsPanel()  // فتح/إغلاق اللوحة
```

**الميزات:**
- ✅ عرض كل الإشعارات المسبقة
- ✅ تحديد تلقائي كمقروء عند الفتح
- ✅ مسح الكل في زر واحد
- ✅ وقت بصيغة عربية (الآن، قبل 5 دقائق، إلخ)
- ✅ لا تختفي عند إغلاق التطبيق

---

### 3. تحديث الأخبار الفوري

```javascript
updateLatestNewsDisplay(allNews)
```

**ماذا يحدث:**
- ✅ أحدث 3 أخبار تُعرض على الصفحة الرئيسية
- ✅ ترتيب تنازلي (أحدث أولاً)
- ✅ أيقونة "عاجل" للأخبار الحرجة
- ✅ صورة الخبر إن وجدت
- ✅ زر "عرض المزيد" يظهر عند الحاجة

---

### 4. نمط الاهتزاز (Vibration Pattern)

```javascript
// للأخبار العادية
vibrate: [200, 100, 200]
// = اهتزاز 200ms ثم توقف 100ms ثم اهتزاز 200ms

// للأخبار العاجلة
vibrate: [300, 100, 300, 100, 300]
// = نمط أقوى وأطول
```

**الأجهزة المدعومة:**
- ✅ جميع هواتف Android
- ✅ بعض أجهزة iOS
- ⚠️ أجهزة الكمبيوتر المدعومة فقط

---

## 💡 كيفية الاستخدام

### استخدام بسيط (تلقائي)

**لا يحتاج إلى أي شيء!** النظام يعمل تلقائياً:

1. عند فتح التطبيق → تحميل الإشعارات
2. طلب إذن الإشعارات تلقائياً
3. بدء مراقبة الأخبار كل 60 ثانية
4. عند نزول خبر جديد → إشعار فوري

---

### إضافة إشعار يدويٍ (من لوحة التحكم)

```javascript
// عند نشر خبر جديد في Admin
const newsItem = {
    title: "خبر جديد",
    content: "محتوى الخبر...",
    image: "رابط الصورة",
    urgent: false,  // أو true للأخبار العاجلة
    created_at: new Date().toISOString()
};

// حفظه في Firebase
await saveData('news', newsItem);

// سيتم اكتشافه تلقائياً في الفحص التالي ← لا حاجة لفعل شيء!
```

---

### التحكم اليدوي (Optional)

```javascript
// فحص الأخبار يدويٍ
await checkForNewNews();

// بدء/إيقاف المراقبة
startNewsMonitoring();
stopNewsMonitoring();

// مسح الإشعارات
clearAllNotifications();

// فتح/إغلاق اللوحة
toggleNotificationsPanel();
```

---

## 🎨 التخصيص والتطوير

### 1. تغيير فترة المراقبة

**الوضع الحالي:** كل 60 ثانية

```javascript
// في script-firebase-complete.js أو script-firebase-fixed.js
// السطر ~24
CONFIG = {
    NEWS_CHECK_INTERVAL: 30000  // ← غيّر إلى 30 ثانية
}
```

**خيارات مشهورة:**
- `10000` - كل 10 ثوان (استهلاك بطارية أكثر)
- `30000` - كل 30 ثانية (توازن)
- `60000` - كل دقيقة (توفير البطارية)
- `120000` - كل دقيقتين (للتطبيقات منخفضة التحديث)

---

### 2. تخصيص صوت الإشعار

```javascript
// في دالة sendBrowserNotification()
const notification = new Notification(title, {
    sound: 'path/to/notification-sound.mp3',
    ...options
});
```

---

### 3. تخصيص الألوان والتأثيرات

```css
/* في styles.css */

/* الخبر العاجل */
.news-item.urgent {
    border-right: 4px solid #ff4444;  /* اللون الأحمر */
    animation: urgentPulse 2s infinite;  /* السرعة */
}

/* تغيير اللون الأساسي للإشعارات */
.news-item {
    border-right: 4px solid #007bff;  /* أزرق بدلاً من رمادي */
}
```

---

### 4. إضافة فئات إشعارات جديدة

```javascript
// مثال: إضافة إشعارات العروض
async function checkForNewOffers() {
    const offers = await getData('offers');
    // نفس المنطق مثل checkForNewNews()
    // لكن مع فئة أخرى
    
    newOffers.forEach(offer => {
        addNotification(
            'عرض جديد: ' + offer.title,
            offer.description,
            'offer'  // ← فئة جديدة
        );
        
        sendBrowserNotification('🏷️ عرض جديد', {
            body: offer.description,
            tag: 'offer-' + offer.id
        });
    });
}

// ثم استدعاء هذه الدالة في initializeNavigation()
startOfferMonitoring();  // مثل startNewsMonitoring()
```

---

## 🔧 حل المشاكل

### المشكلة 1: الإشعارات لا تظهر على الهاتف

**الأسباب:**
1. لم يُعطَ الإذن للتطبيق
2. الهاتف في وضع "عدم الإزعاج"
3. إعدادات البطارية تحظر الإشعارات

**الحل:**
```
الهاتف ← الإعدادات ← التطبيقات ← التطبيق
← الإشعارات ← تفعيل الإشعارات
```

---

### المشكلة 2: الاهتزاز لا يعمل

**الأسباب:**
1. المتصفح لا يدعم Vibration API
2. الهاتف في وضع الصامت
3. إعدادات التطبيق تحظر الاهتزاز

**الحل:**
```javascript
// تحقق من الدعم
if ('vibrate' in navigator) {
    navigator.vibrate([200, 100, 200]);
} else {
    console.log('Vibration API not supported');
}
```

---

### المشكلة 3: الإشعارات متكررة

**السبب:** `lastNewsCheckTime` لم يُحدث بشكل صحيح

**الحل:**
```javascript
// في Browser DevTools Console
lastNewsCheckTime = Date.now();
localStorage.removeItem('notifications');
// ثم أعد تحميل الصفحة
```

---

### المشكلة 4: عدم ظهور الأخبار الجديدة على الصفحة

**السبب:** `updateLatestNewsDisplay()` قد لا تكون في الصفحة المعروضة

**الحل:**
```javascript
// تأكد من استدعاء:
if (currentPage === 'home' || currentPage === 'news') {
    updateLatestNewsDisplay(news);
}
```

---

### المشكلة 5: الإشعارات لا تُحفظ

**السبب:** `localStorage` قد يكون معطلاً

**الحل:**
```javascript
// في Console
try {
    localStorage.setItem('test', 'test');
    localStorage.removeItem('test');
    console.log('localStorage متاح ✓');
} catch(e) {
    console.error('localStorage معطل:', e);
}
```

---

## 📊 مثال كامل للعملية

```
[المستخدم يفتح التطبيق]
    ↓
DOMContentLoaded event يعمل
    ↓
1. loadNotificationsFromStorage() 
   → تحميل الإشعارات القديمة من localStorage
    ↓
2. requestNotificationPermission()
   → طلب إذن الإشعارات (ظهور pop-up)
    ↓
3. startNewsMonitoring()
   → بدء فحص الأخبار
    ↓
[بعد 60 ثانية]
    ↓
checkForNewNews() تعمل
    ↓
جلب الأخبار من Firebase
    ↓
المقارنة: الخبر أحدث من lastNewsCheckTime؟
    ↓ نعم
    ↓
[يحدث]
├─ إضافة للوحة الداخلية (addNotification)
├─ إرسال إشعار خارجي (sendBrowserNotification)
│   ├─ ظهور البوب آب
│   ├─ صوت تنبيه
│   └─ اهتزاز الهاتف
└─ تحديث الأخبار على الصفحة (updateLatestNewsDisplay)

[إذا كان على الصفحة الرئيسية]
    ↓
يرى الخبر الجديد مباشرة في أعلى قائمة الأخبار
    ↓
شارة الإشعارات تظهر برقم (1)
    ↓
عند النقر على الشارة ← فتح لوحة الإشعارات
    ↓
الإشعار يُؤشر كمقروء
    ↓
الشارة تختفي
```

---

## 🚀 الميزات المستقبلية (TODO)

- [ ] فئات إشعارات متعددة (أخبار، عروض، تنبيهات)
- [ ] تصفية الإشعارات حسب النوع
- [ ] إعدادات المستخدم للتحكم بالإخطارات
- [ ] رابط مباشر من الإشعار للخبر الكامل
- [ ] إشعارات مخصصة per user
- [ ] تقارير عن تفاعل المستخدم مع الإشعارات

---

## 📝 ملاحظات مهمة

⚠️ **الأداء:**
- المراقبة تعمل كل 60 ثانية (قابل للتخصيص)
- لا تستهلك موارد كبيرة على الجهاز
- يمكن إيقافها إذا لم يكن التطبيق نشطاً

⚠️ **الخصوصية:**
- الإشعارات قد تظهر على شاشة القفل
- لا تضع بيانات حساسة في نص الإشعار
- احترم إذن المستخدم

⚠️ **التوافقية:**
```
✅ Chrome 22+
✅ Firefox 22+
✅ Safari 6+
✅ Edge 14+
✅ Android Browser
⚠️ IE غير مدعوم
```

---

## 📞 الدعم والمساعدة

**للتحقق من الأخطاء:**
1. فتح DevTools (F12)
2. انظر إلى Console للأخطاء
3. تحقق من:
   - `notifications` array
   - `localStorage`
   - `Notification.permission`
   - `Firebase connection`

**للاختبار:**
```javascript
// في Console
// 1. تحقق من المتغيرات
console.log('Notifications:', notifications);
console.log('Count:', notificationCount);
console.log('Permission:', Notification.permission);

// 2. اختبر فحص الأخبار
await checkForNewNews();

// 3. اختبر الإشعار
sendBrowserNotification('اختبار', {body: 'تجربة الإشعار'});
```

---

**آخر تحديث:** 13 يناير 2026  
**الإصدار:** 2.0  
**الحالة:** ✅ مكتمل وجاهز للإنتاج
